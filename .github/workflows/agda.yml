name: Agda-Validation-PSIU

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    container:
      image: agda/agda:latest # Usa l'ambiente ufficiale Agda
    steps:
      - uses: actions/checkout@v4
      - name: Run Agda
        run: agda --no-libraries agdatest.agda
