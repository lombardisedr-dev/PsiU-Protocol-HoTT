name: "PsiU: Extreme Topological Invariance"

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  topology-check:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install Dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::testthat
            any::remotes

      - name: Install PsiUEngineRL (Local)
        run: R CMD INSTALL .

      - name: Run Topological Validation
        shell: Rscript {0}
        run: |
          library(testthat)
          library(PsiUEngineRL)
          # Nome file aggiornato per riflettere lo stress test
          source("extreme_topological_invariance_test.R")
