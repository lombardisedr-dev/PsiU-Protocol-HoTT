# PsiU-Protocol-HoTT

A rigorous framework for the verification of semi-simplicial types and $n$-simplex stability under the Homotopy Type Theory (HoTT) Univalence Axiom, using S5 Alethic Modal Logic.

## 1. Theoretical Framework (HoTT S5)

The **PsiU-Protocol-HoTT** reduces logical entropy by mapping continuous input spaces onto a single fixed geometric attractor: the **Gnomonic Ratio** ($G \approx 0.618034$). 

The core logical engine (`psiu_hott_engine`) executes a deterministic quantization via a geometric proximity operator (`create_path`), assigning a formal identity proof (*"Reflexivity_Path"*) to each point based on strict modal boundaries:

*   **Necessity ($\square$) — BOX:** Distance $\le 0.002$. Absolute formal resonance.
*   **Possibility ($\diamond$) — DIAMOND:** Distance $0.002 < \Delta \le 0.010$. Controlled contingency.
*   **Accident — NOISE:** Distance $> 0.010$. Broken homotopic reflexivity.

---

## 2. Benchmark & Core Results

The protocol independently evaluates structural and clinical distributions, providing high-selectivity filtering and elite computational efficiency.

### Run 1: UCI Cardiotocography Validation (2,126 Samples)
Models bimodal fetal heart-rate distributions ($1,650$ baseline samples vs. $476$ clinical anomalies) scaled around the gnomonic attractor $G$.
*   **BOX (Necessity):** $5.46\%$ ($116$ samples)
*   **DIAMOND (Possibility):** $41.48\%$ ($882$ samples)
*   **NOISE (Accident):** $53.06\%$ ($1,128$ samples)
*   **Shannon Information Entropy:** $1.2407$ bits
*   **Noise Peak Mode (Critical Point):** $0.010615$

*Verdict:* Maximum architectural honesty. The validator strictly rejects over half the dataset as external stochastic noise, refusing artificial data fitting.

### Run 2: Dual-Method Challenge (10,600 Samples)
A direct comparison between the HoTT Geometric Approach (Topological Density via local $k$-NN) and the classical industry standard (Isolation Forest via `isotree`).

