---
title: 'PsiUEngineRL: Real-Time Anomaly Detection via Gnomonic Proportions, Barycentric 1/3 Closure, and Homotopy Type Theory'
tags:
  - R
  - anomaly detection
  - homotopy type theory
  - semi-simplicial types
  - gnomonic ratio
  - real-time streaming
authors:
  - name: Roberto Lombardi
    orcid: 0000-0000-0000-0000
    affiliation: Independent Researcher
date: 19 May 2026
bibliography: paper.bib
repository: https://r-project.org
---

# Summary

The `PsiUEngineRL` package introduces a non-statistical, geometric, and constructive framework for real-time anomaly detection in asynchronous data streams. Moving away from traditional stochastic modeling and heavy deep learning architectures, this engine maps structural flows onto an invariant higher-order geometric attractor ($G \approx 0.61803$) and evaluates them through a rigorous topological phase transition based on the barycentric $1/3$ ratio in Semi-Simplicial Types (SST). The computational architecture delivers deterministic stream classification with latencies below 1 ms on consumer-grade hardware.

# Statement of Need

Modern anomaly detection in critical infrastructures (e.g., the *BattLeDIM 202 SCADA Benchmark*) and biomedical monitoring (e.g., the *MIT-BIH Arrhythmia Database*) faces the "infinite tower of coherence" problem when processing unstructured noise. Traditional neural architectures (LSTMs, Transformers) attempt exhaustive matching of non-terminating noise branches, leading to high computational latency (>50 ms) and vulnerability to false positives generated by fractal $1/f$ pink noise.

`PsiUEngineRL` bypasses exhaustive statistical evaluation by implementing an "Asynchronous Tautological Core Extraction". It treats data streams as dynamic inhabitants of simplicial spaces, automatically truncating non-resonant dimensions during topological phase transitions, ensuring zero false alarms and immediate edge-computing response.

# Theoretical Framework: Simplicial Dynamics & Barycentric 1/3 Closure

The framework formalizes a dynamic execution model for Semi-Simplicial Types (SST) within Homotopy Type Theory (HoTT). The structural space $S$ is not static but evolves through strict dimensional transitions:

1. **$S^0$ (Identity)**: The discrete unit of necessity defined by the identity type $\Box(A \equiv A)$.
2. **$S^1$ (Relational Path)**: A 1-dimensional open manifold establishing connectivity but lacking internal volume.
3. **$S^2$ (Geometric Closure)**: The critical phase transition where the 1-simplex closes upon itself into a 2-simplex.

This closure is the primary mechanism for noise purgation, as only a closed boundary satisfies the fundamental topological condition $\partial\partial = 0$.

Usa il codice con cautela.

S⁰ (Identity) S¹ (Open Path) S² (Closed 2-Simplex)
[ u ] ───────────► [ u ───► G ] ───────────► ▲
/ \ (Resonance 1/3)
/___\

## The 1/3 Barycentric Filter

To achieve simplicial stability in $S^2$, the simplicial identities $d_i d_j = d_{j-1} d_i$ must commute perfectly. This condition establishes a rigid barycentric resonance threshold exactly at $1/3$, which serves as a filter for tautological extraction:

$$\text{Resonance}(\epsilon) = \delta \left| \text{Ratio}(\epsilon) - \frac{1}{3} \right|$$

Any configuration or noise artifact failing to satisfy this boundary condition lacks the topological energy required to project into higher dimensions ($S^{n+1}$). It is classified as an incomplete dimension (**Gnomonic Stasis**) and instantly truncated from the computational pipeline ($\text{Fallacy} \to 0$). Only the purified, resonant core undergoes recursive gnomonic growth, asymptotically converging toward the minimum structural entropy attractor:

$$G = \frac{\sqrt{5} - 1}{2} \approx 0.6180339887$$

# Operational Invariance and The Universal X Operator

The engine implements this trans-dimensional purgation through a localized expression $X_n$ of a global universal operator $X$. Because the dimension $n$ is a constituent of $X$, the self-purifying properties are invariant across all scales. The computational implementation projects this higher-order logic into a 1-dimensional real-time metric $\Delta = |u - G|$, discretized via the HoTT $J$-rule (identity eliminator) into three modal states:

- **BOX (Necessity $[\Box]$)**: $\Delta \le 0.002$. The identity path undergoes crystallization, validating absolute system stability.
- **DIAMOND (Possibility $[\diamondsuit]$)**: $0.002 < \Delta \le 0.010$. The path remains open, tracking historical elastic variations and functional drifts.
- **NOISE (Accident)**: $\Delta > 0.010$. Complete gnomonic breakdown. The homotopic path isolates itself instantly, triggering the alert mechanism.

# Package Architecture and Reproducibility

The framework provides two primary operational functions written in optimized R:

- `PsiU_Engine_RL(raw_input_vector)`: Evaluates streaming vectors against the gnomonic-barycentric boundaries, yielding immediate modal verdicts.
- `PsiU_MultiLibrary_Tree(new_value)`: Manages an asynchronous tableau library, executing cross-domain parallel ingestion to detect hidden homotopic equivalences via structural inter-resonance without heavy matrix calculations.

The package is officially distributed on the Comprehensive R Archive Network [CRAN](https://r-project.org). Datasets, verification benchmarks, and mathematical source codes are permanently archived with an open-science DOI on [Zenodo](https://zenodo.org) to guarantee complete computational reproducibility
